---
"@ian-pascoe/pi-skills-selector": minor
---

Add native `$skill-name` autocomplete and source-preserving Skill document links through Pi's user-input hook, including terminal and RPC `prompt` steering/follow-ups. Document the Pi 0.85.1 limitation for direct RPC `steer` and `follow_up` commands.
